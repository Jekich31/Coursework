<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="{DynamicResource m_WindowTitle}" Height="750" Width="1200"
        Background="#F4F6F9" WindowStartupLocation="CenterScreen">

    <Window.Resources>
 
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                        CornerRadius="12" 
                        Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"
                                      TextElement.FontWeight="{TemplateBinding FontWeight}"
                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#FAFAFA"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Border Background="White" CornerRadius="15" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="1" Color="#000000" Opacity="0.04"/>
                                </Border.Effect>
                            </Border>
                            <StackPanel Margin="10">
                                <ContentPresenter ContentSource="Header" 
                  TextElement.FontWeight="Bold" 
                  TextElement.Foreground="#2C3E50" 
                  Margin="5,0,0,10"/>
                                <ContentPresenter />
                            </StackPanel>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="Transparent" Margin="0,0,0,10">
            <MenuItem Header="{DynamicResource m_Language}">
                <MenuItem Header="Українська" Command="{Binding ChangeLanguageCommand}" CommandParameter="UKR"/>
                <MenuItem Header="English" Command="{Binding ChangeLanguageCommand}" CommandParameter="EN"/>
            </MenuItem>
        </Menu>

        <GroupBox Grid.Row="1" Header="{DynamicResource m_AddTitle}" Margin="0,0,0,15" Padding="10">
            <WrapPanel Orientation="Horizontal">
                <StackPanel Margin="5">
                    <TextBlock Text="{DynamicResource m_Vin}" Margin="2,0,0,3" FontSize="11" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding Vin, UpdateSourceTrigger=PropertyChanged}" Width="110"/>
                </StackPanel>

                <StackPanel Margin="5">
                    <TextBlock Text="{DynamicResource m_Type}" Margin="2,0,0,3" FontSize="11" Foreground="#7F8C8D"/>
                    <ComboBox ItemsSource="{Binding VehicleTypes}" SelectedItem="{Binding SelectedVehicleType}" Width="130" Height="30"/>
                </StackPanel>

                <StackPanel Margin="5">
                    <TextBlock Text="{DynamicResource m_Brand}" Margin="2,0,0,3" FontSize="11" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding Brand, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                </StackPanel>

                <StackPanel Margin="5">
                    <TextBlock Text="{DynamicResource m_Model}" Margin="2,0,0,3" FontSize="11" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding Model, UpdateSourceTrigger=PropertyChanged}" Width="100"/>
                </StackPanel>

                <StackPanel Margin="5">
                    <TextBlock Text="{DynamicResource m_Latitude}" Margin="2,0,0,3" FontSize="11" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding Latitude, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                </StackPanel>

                <StackPanel Margin="5">
                    <TextBlock Text="{DynamicResource m_Longitude}" Margin="2,0,0,3" FontSize="11" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding Longitude, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                </StackPanel>

                <StackPanel Margin="5">
                    <TextBlock Text="{DynamicResource m_Capacity}" Margin="2,0,0,3" FontSize="11" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding Capacity, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                </StackPanel>

                <Button Content="{DynamicResource m_AddButton}" Command="{Binding AddVehicleCommand}" 
                        Margin="10,18,5,5" Padding="15,6" Background="#2ECC71"/>
            </WrapPanel>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="{DynamicResource m_Refresh}" Command="{Binding LoadVehiclesCommand}" Margin="0,0,10,0" Background="#34495E"/>
            <Button Content="{DynamicResource m_Delete}" Command="{Binding DeleteVehicleCommand}" Background="#E74C3C"/>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <ListBox ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}" 
                         Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="5"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="SelectionBorder" 
                            CornerRadius="18" 
                            Padding="3" 
                            Background="Transparent"
                            BorderThickness="2"
                            BorderBrush="Transparent">
                                            <ContentPresenter />
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="SelectionBorder" Property="Background" Value="#153498DB"/>
                                            </Trigger>

                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="SelectionBorder" Property="BorderBrush" Value="#3498DB"/>
                                                <Setter TargetName="SelectionBorder" Property="Background" Value="#203498DB"/>
                                                <Setter TargetName="SelectionBorder" Property="Effect">
                                                    <Setter.Value>
                                                        <DropShadowEffect BlurRadius="12" ShadowDepth="0" Color="#3498DB" Opacity="0.4"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Width="280" Background="White" CornerRadius="16" Padding="15" Margin="5">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="12" ShadowDepth="3" Direction="270" Color="#000000" Opacity="0.07"/>
                                </Border.Effect>

                                <StackPanel>
                                    <DockPanel Margin="0,0,0,8">
                                        <StackPanel DockPanel.Dock="Left">
                                            <TextBlock Text="{Binding Brand}" FontSize="16" FontWeight="Bold" Foreground="#2C3E50"/>
                                            <TextBlock Text="{Binding Model}" FontSize="13" Foreground="#7F8C8D"/>
                                        </StackPanel>

                                        <Border DockPanel.Dock="Right" CornerRadius="10" Padding="8,4" VerticalAlignment="Top">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#E74C3C"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Engine}" Value="Running">
                                                            <Setter Property="Background" Value="#2ECC71"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>

                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="6" Height="6" Fill="White" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Engine}" FontSize="11" FontWeight="Bold" Foreground="White"/>
                                            </StackPanel>
                                        </Border>
                                    </DockPanel>

                                    <Separator Background="#ECF0F1" Margin="0,0,0,8"/>

                                    <UniformGrid Columns="2" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Text="VIN" FontSize="10" Foreground="#BDC3C7" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Vin}" FontSize="11" Foreground="#34495E"/>
                                        </StackPanel>
                                        <StackPanel>
                                            <TextBlock Text="{DynamicResource m_ColLocation}" FontSize="10" Foreground="#BDC3C7" FontWeight="Bold"/>
                                            <TextBlock Text="{Binding LocationData}" FontSize="11" Foreground="#34495E"/>
                                        </StackPanel>
                                    </UniformGrid>

                                    <TextBlock Text="{DynamicResource m_ColLevel}" FontSize="10" Foreground="#7F8C8D" Margin="0,0,0,2"/>
                                    <ProgressBar Value="{Binding CurrentLevel, Mode=OneWay}" Maximum="{Binding Capacity, Mode=OneWay}" 
                                                 Height="8" Foreground="#2ECC71" Background="#ECF0F1" BorderThickness="0" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding CurrentLevel}" FontSize="10" HorizontalAlignment="Right" Foreground="#95A5A6" Margin="0,0,0,8"/>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="{DynamicResource m_ColDoors}" FontSize="11" Foreground="#7F8C8D" Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding Doors}" FontSize="11" FontWeight="Bold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="#E67E22"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Doors}" Value="Locked">
                                                            <Setter Property="Foreground" Value="#27AE60"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <UniformGrid Columns="2">
                                        <Button Content="{DynamicResource m_ToggleEngine}" 
                                                Command="{Binding DataContext.ToggleEngineCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Background="#3498DB" Padding="4" Margin="0,0,3,0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <Button Content="{DynamicResource m_ToggleDoors}" 
                                                Command="{Binding DataContext.ToggleDoorsCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"
                                                Background="#E67E22" Padding="4" Margin="3,0,0,0">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>

            <Border Grid.Column="1" Margin="10,5,5,5" CornerRadius="16" ClipToBounds="True" Background="White">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="3" Direction="270" Color="#000000" Opacity="0.07"/>
                </Border.Effect>
                <wv2:WebView2 x:Name="mapWebView" />
            </Border>
        </Grid>
    </Grid>
</Window>